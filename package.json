{
  "name": "cloudflare-os-starter",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "test": "node --test scripts/*.test.mjs && pnpm --filter custom-gatekeeper test && pnpm --filter error-reporter test",
    "build": "pnpm --filter custom-gatekeeper build && pnpm --filter error-reporter build",
    "typecheck": "pnpm --filter custom-gatekeeper types:check && pnpm --filter error-reporter types:check",
    "lint": "pnpm typecheck",
    "check": "node scripts/deploy.mjs --check --config deployment.dry-run.jsonc",
    "check:deployment": "node scripts/deploy.mjs --check",
    "deploy": "node scripts/deploy.mjs"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.20.1",
    "jsonc-parser": "^3.3.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.115.0"
  }
}
